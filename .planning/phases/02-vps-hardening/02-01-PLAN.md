---
phase: 02-vps-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/kerntune/kerntune.go
  - internal/kerntune/kerntune_linux.go
  - internal/kerntune/kerntune_other.go
  - internal/kerntune/kerntune_test.go
  - internal/git/git.go
  - internal/git/git_test.go
  - internal/pipeline/pipeline.go
  - internal/pipeline/pipeline_test.go
  - cmd/vlessfilter/main.go
autonomous: true
requirements:
  - TEST-02
  - DEP-03
  - DEP-04
  - DEP-05
must_haves:
  truths:
    - "kerntune.Apply() raises RLIMIT_NOFILE to 100k on Linux and tries 2 sysctl writes; logs WARN on each individual failure but never returns error"
    - "Pipeline.Run respects --budget-min: when deadline expires mid-run, in-flight xray-knife processes are SIGTERM'd and select stage still runs to ship partial output"
    - "When --git-push is true and $GH_TOKEN is set, pipeline.Run commits + pushes outputs at every checkpoint AND at end of run; secrets never land in .git/config"
  artifacts:
    - "internal/kerntune package with Linux + non-Linux build tags"
    - "internal/git package with Configure, CommitAll, Push functions"
    - "Pipeline accepts BudgetMin, CheckpointMin, GitPush, GitBranch, GitToken in Opts"
    - "main.go has --budget-min, --checkpoint-min, --git-push, --git-branch flags"
---

<objective>
Add the three pillars of unattended ephemeral-VPS execution: kernel tuning, wall-clock budget, and git-checkpoint loop.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-vps-hardening/02-CONTEXT.md
@.planning/phases/02-vps-hardening/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: internal/kerntune package</name>
  <files>internal/kerntune/kerntune.go, internal/kerntune/kerntune_linux.go, internal/kerntune/kerntune_other.go, internal/kerntune/kerntune_test.go</files>
  <action>
    Create `internal/kerntune` package. `Apply(ctx)` is OS-dispatched via build tags:
    - kerntune_linux.go (build tag `linux`): raise RLIMIT_NOFILE to 100k via syscall.Setrlimit (best-effort), then `exec.CommandContext("sysctl", "-w", "net.ipv4.tcp_tw_reuse=1")` and same for `net.ipv4.ip_local_port_range="1024 65535"`. Each step independently logs slog.Warn on failure. Returns nil always (best-effort).
    - kerntune_other.go (build tag `!linux`): no-op, slog.Debug "skipping kernel tuning on non-Linux".
    - Public API: `func Apply(ctx context.Context) error` (always returns nil currently; signature future-proofs for fatal errors).
    Test: only Linux test that asserts RLIMIT_NOFILE was raised (or unchanged if already higher).
  </action>
  <verify>go test ./internal/kerntune -count=1</verify>
  <acceptance_criteria>
    - kerntune.go exports Apply
    - kerntune_linux.go contains "syscall.Setrlimit" and "tcp_tw_reuse"
    - kerntune_other.go has build tag !linux
    - go build ./... passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: internal/git package</name>
  <files>internal/git/git.go, internal/git/git_test.go</files>
  <action>
    Create `internal/git` package. Functions:
    - `func Configure(repoDir, name, email string) error` — sets local user.name/user.email if missing
    - `func CommitAll(repoDir, msg string) (committed bool, err error)` — git add -A; git diff --cached --quiet (returns committed=false on no-changes); else git commit -m
    - `func Push(repoDir, branch, token string) error` — runs `git -c http.extraheader="Authorization: Basic $(b64 oauth2:TOKEN)" push origin BRANCH`
    All shell out via os/exec; no external git library.
    Test: TestEncodeAuthHeader (verify base64 of "oauth2:abc" round-trips correctly), TestCommitAll_NoChanges_ReturnsFalse using a real `git init` in t.TempDir().
  </action>
  <verify>go test ./internal/git -count=1</verify>
  <acceptance_criteria>
    - git.go contains "http.extraheader" string literal
    - git.go contains "oauth2:" string literal
    - tests pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Pipeline budget + checkpoint + git-push integration</name>
  <files>internal/pipeline/pipeline.go, internal/pipeline/pipeline_test.go, cmd/vlessfilter/main.go</files>
  <action>
    Extend `pipeline.Opts` with: BudgetMin int, CheckpointMin int, GitPush bool, GitBranch string, GitToken string, GitRepoDir string.
    In Run:
    1. If BudgetMin > 0, wrap ctx with WithDeadline(now + budget).
    2. If full pipeline (Stage == ""), call kerntune.Apply at the top of runTest.
    3. After fetch, before stage 2, start a checkpoint goroutine that calls runSelect every CheckpointMin minutes; on success and GitPush, calls git.CommitAll + git.Push.
    4. On context.Done before stages complete, log warn "budget reached", run final select+commit+push, return nil (success — partial results shipped).
    main.go: add --budget-min (default 55), --checkpoint-min (default 2), --git-push (default false), --git-branch (default "main"), --git-repo (default "."). Read $GH_TOKEN from env.
    Tests: extend pipeline_test.go with TestRun_BudgetExpires_StillWritesOutput using a fakeRunner that sleeps and a tiny budget.
  </action>
  <verify>go test ./internal/pipeline -count=1 AND go build ./cmd/vlessfilter</verify>
  <acceptance_criteria>
    - pipeline.go imports kerntune and git packages
    - pipeline.go contains "WithDeadline" or "WithTimeout"
    - main.go contains "--budget-min" flag (or "budget-min" identifier)
    - main.go reads "GH_TOKEN" env var
    - all tests pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
- go test ./internal/... -count=1 passes
- go build ./cmd/vlessfilter passes
- ./bin/vlessfilter run --help lists --budget-min, --checkpoint-min, --git-push
</verification>

<success_criteria>All tasks done; binary supports budget-bounded run with optional auto-commit.</success_criteria>

<output>SUMMARY at .planning/phases/02-vps-hardening/02-SUMMARY.md</output>
<!-- Was 02-01-SUMMARY.md, which never existed: both plans were written up in a single combined 02-SUMMARY.md. -->
