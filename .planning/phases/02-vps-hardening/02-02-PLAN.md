---
phase: 02-vps-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - internal/output/output.go
  - internal/output/output_test.go
  - internal/sources/sources.go
  - internal/sources/sources_test.go
  - internal/xrayknife/xrayknife.go
  - internal/pipeline/pipeline.go
  - sources.yaml
autonomous: true
requirements:
  - AGGR-02
  - AGGR-05
  - SEL-04
  - OUT-03
  - OUT-04
  - OUT-05
  - TEST-05
must_haves:
  truths:
    - "After a run, all-results.csv exists with header row + one row per tested key, sorted by Country asc then Score desc (deterministic)"
    - "After a run, raw/dead.txt exists with one dead-key vless URI per line (latency=0 or >10000ms)"
    - "Two consecutive runs against the same DB produce byte-identical README.md (apart from a single timestamp comment) and byte-identical subs/<CC>.txt files"
    - "Default sources.yaml when countries is [] expands to a hardcoded list of 100+ ISO codes that v2go covers"
    - "xray-knife progress-bar carriage-return lines are filtered out of stdout in non-TTY runs"
  artifacts:
    - "output.WriteDiagnostics(outDir, results) writes all-results.csv + raw/dead.txt"
    - "sources.go has DefaultCountries() returning 100+ ISO codes"
    - "xrayknife runSubprocess helper drops \\r-terminated progress lines"
---

<objective>
Final layer of hardening: diagnostic CSV/dead-list outputs, deterministic ordering everywhere, default-country expansion, and quiet-mode for xray-knife in non-TTY runs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/02-vps-hardening/02-CONTEXT.md
@.planning/phases/02-vps-hardening/02-RESEARCH.md
@.planning/phases/02-vps-hardening/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnostic outputs in internal/output</name>
  <files>internal/output/output.go, internal/output/output_test.go</files>
  <action>
    Add `WriteDiagnostics(outDir string, all []selector.Result, dead []selector.Result) error` to internal/output:
    - all-results.csv: header "Link,LatencyMs,SpeedMbps,Country,Score"; rows sorted by Country asc, Score desc, Link asc (full deterministic).
    - raw/dead.txt: links of dead results sorted alphabetically, one per line.
    Update `Write` to also accept (or wrap into) a unified `WriteAll` that calls both. Pipeline calls `WriteAll`.
    Update README.md template: timestamp moves into HTML comment at the bottom (`<!-- last-tested: ... -->`); table uses no time column. This makes README byte-identical when results don't change.
    Add tests: TestWriteDiagnostics_DeterministicOrdering, TestWriteAll_TimestampInComment.
  </action>
  <verify>go test ./internal/output -count=1</verify>
  <acceptance_criteria>
    - output.go exports WriteDiagnostics and WriteAll
    - output.go contains "Link,LatencyMs,SpeedMbps,Country,Score" csv header literal
    - output.go contains "<!-- last-tested:" comment marker
    - tests pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Default-country expansion + sources.yaml refresh</name>
  <files>internal/sources/sources.go, internal/sources/sources_test.go, sources.yaml</files>
  <action>
    Add `DefaultCountries() []string` returning ~100 ISO codes that v2go is known to publish (US, DE, GB, FR, NL, JP, RU, CN, KR, SG, ...). When `cfg.Countries` is nil/empty AND any source has kind: country-template, use DefaultCountries().
    Update sources.yaml: countries: [] (empty = all defaults), and ship 2 sources: v2go-country (template) + a verified-working aggregator. Replace mahdibland with `Epodonios/v2ray-configs/main/Sub1.txt` (we'll add a source and verify).
    Add test: TestExpand_EmptyCountriesUsesDefaults.
  </action>
  <verify>go test ./internal/sources -count=1; curl -sI Epodonios sub URL returns 200</verify>
  <acceptance_criteria>
    - sources.go exports DefaultCountries
    - DefaultCountries returns at least 50 entries
    - sources.yaml has countries: [] and at least 2 enabled sources
    - tests pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: xray-knife quiet wrapper</name>
  <files>internal/xrayknife/xrayknife.go</files>
  <action>
    Add `streamFiltered` helper that wraps an io.Writer and drops any byte run terminated by `\r` (the progress-bar update line). For HTTPTest and SubsFetch, when xray-knife is invoked, set `cmd.Stdout`/`cmd.Stderr` to streamFiltered(os.Stderr) so multi-line important output (table headers, completion summary) still shows but the spinning progress bar doesn't pollute logs.
    Auto-detect: only filter when stdout is not a TTY (use `term.IsTerminal(int(os.Stderr.Fd()))` or check `$CI` / `$VLESSFILTER_QUIET` env). On TTY, pass through unchanged so devs see the bar.
  </action>
  <verify>go build ./...</verify>
  <acceptance_criteria>
    - xrayknife.go has streamFiltered or filterCarriageReturns helper
    - go build passes
  </acceptance_criteria>
</task>

</tasks>

<verification>
- go test ./internal/... -count=1 passes
- go build ./cmd/vlessfilter passes
- 2 consecutive --stage select runs against same DB produce byte-identical README.md (modulo the bottom comment) and byte-identical subs/<CC>.txt files (verified via `diff -q`)
</verification>

<success_criteria>Phase 2 fully complete; all 11 phase requirements addressed.</success_criteria>

<output>SUMMARY at .planning/phases/02-vps-hardening/02-SUMMARY.md</output>
<!-- Was 02-02-SUMMARY.md, which never existed: both plans were written up in a single combined 02-SUMMARY.md. -->
